resources:
  repositories:
    - repository: wiki # work around for security issue for pipelines to access the WIKI repo
      type: git
      name: Stacks/Stacks.Wiki
      ref: wikiMaster

parameters:
  text: ''

jobs: 
- job: Hello
  steps:
    - script: |
        echo "Hello, World! ${{ parameters.text }}"
      displayName: 'Print Hello World'