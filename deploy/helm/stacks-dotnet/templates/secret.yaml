apiVersion: v1
kind: Secret
metadata:
  annotations:
    {{- include "stacks-dotnet.annotations" . | nindent 8 }}
  labels:
    {{- include "stacks-dotnet.labels" . | nindent 8 }}
  name: {{ include "stacks-dotnet.secretName" . }}
  namespace: {{ .Values.namespace }}
type: Opaque
data:
  blank: blank
# {{- if eq .Values.logging.target "appinsights" }}
#   app_insights: {{ .Values.logging.appInsightsKey | b64enc | quote }}
# {{- end}}
# {{- if eq .Values.logging.target "cloudwatch" }}
#   CLOUDWATCH_LOG_GROUP: {{ .Values.logging.cloudwatchLogGroup | b64enc | quote  }}
#   CLOUDWATCH_STREAM_PREFIX: {{.Values.logging.cloudwatchStreamPrefix | b64enc | quote }}}
# {{- end}}
  